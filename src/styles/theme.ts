export const theme = {
  colors: {
    primary: "#007BFF",
    secondary: "#6C757D",
    success: "#28A745",
    danger: "#DC3545",
    // 추가 색상 설정
  },
  fonts: {
    primary: "Helvetica, Arial, sans-serif",
    // 추가 글꼴 설정
  },
  // 추가 스타일 설정
};

export type Theme = typeof theme;
